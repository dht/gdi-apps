export const itemStructure = `export type IProject = {
    id: string;
    key: string;
    name: string;
    avatarUrls?: IAvatarUrls;
    projectType?: string;
    color?: string;
    order?: number;
    tags: string[];
};
`;
